# JusticeLink Mobile

Expo + React Native scaffold for the JusticeLink mobile client.

## Current status
- Expo shell created.
- Role-based navigation placeholders created.
- Mobile auth bootstrap is still a stub.

## Environment
Copy `.env.example` to `.env` and fill in the Supabase values.

## Next step
Wire session restore and role detection into `src/shared/MobileAuthContext.tsx`.